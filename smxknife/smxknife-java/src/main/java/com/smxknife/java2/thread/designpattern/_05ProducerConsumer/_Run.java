package com.smxknife.java2.thread.designpattern._05ProducerConsumer;

/**
 * @author smxknife
 * 2019/9/25
 */
public class _Run {
	public static void main(String[] args) {
		// Producer Consumer模式：我来做，你来用

		/** 相关的设计模式
		 * 1. Mediator模式：多个操作角色之间不相互通信，而是通过中间角色来协调控制，在例子中Consumer和Processor不直接通信，而是通过中间的Channel来处理
		 * 2. Worker Thread模式：由Client角色将数据传递给Worker角色，不过焦点在于共享数据处理的线程，以及降低线程启动时资源消耗
		 * 3. Command模式：
		 * 4. Strategy模式：
		 */
	}
}
