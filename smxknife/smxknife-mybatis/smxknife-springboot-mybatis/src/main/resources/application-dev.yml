spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/test?characterEncoding=utf-8&autoReconnect=true&useSSL=false

mybatis:
  mapper-locations: classpath:/mapper/*Mapper.xml
  type-aliases-package: com.smxknfie.mybatis.springboot.model
logging:
  level:
    com:
      smxknife:
        mybatis:
          springboot:
            model: debug
